import { EditorLayoutClient } from '@/features/cv-editor/components/EditorLayoutClient';

export default function CvEditorPage() {
  return <EditorLayoutClient />;
}

export const metadata = {
  title: 'CV Editor',
  description: 'Build and edit your resume with a live preview.',
};
