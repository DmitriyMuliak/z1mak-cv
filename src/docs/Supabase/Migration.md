# 🧐 Міграції

- Коли ви працюєте в хмарному Supabase (на `supabase.com`), ви можете просто клікати в Studio, створювати таблиці, і все працює.
- Але коли у вас є **локальна** база і **хмарна** (production) база, вам потрібен спосіб переконатися, що їхні структури (схеми) однакові.

**Міграції** — це набір SQL-файлів у вашій папці `.supabase/migrations`, кожен з яких описує _одну зміну_ в базі даних (наприклад, `CREATE TABLE posts`, `ADD COLUMN email TO users` тощо).

Коли ви "застосовуєте" ці міграції до бази даних, вони виконуються по черзі, приводячи базу у потрібний стан.

---

## 🚀 Що робить `supabase db diff`?

Ваше питання було: **"це порівняння з чим?"**

**Коротка відповідь:** `supabase db diff` порівнює **стан вашої локальної бази даних** (яку ви запустили через `supabase start`) з **вашими файлами міграцій** (тобто з тим, що Supabase _вважає_ "офіційною" схемою вашого проєкту).

Ця команда "бачить" зміни, які ви зробили у своїй локальній базі даних, але _ще не записали_ у файл міграції.

### 📝 Практичний робочий процес (Workflow)

Ось як це зазвичай виглядає, коли ви хочете створити нову таблицю `profiles`:

#### Крок 1: Внесіть зміни у локальну базу

Найпростіший спосіб — використати локальну Studio.

1.  Переконайтеся, що Supabase запущено: `supabase start`
2.  Відкрийте локальну Studio у браузері (зазвичай `http://127.0.0.1:54323`).
3.  Використовуючи інтерфейс, створіть нову таблицю. Наприклад, `profiles` з колонками `id` (primary key, uuid) та `username` (text).

На цьому етапі ваша **локальна** база має таблицю `profiles`, але ваші **файли міграцій** про неї нічого не знають, і ваша **хмарна** (production) база її також не має.

#### Крок 2: Згенеруйте файл міграції

Тепер ви кажете Supabase CLI: "Подивись на мою локальну базу, знайди зміни, яких ще немає в міграціях, і створи для мене новий SQL-файл".

Виконайте в терміналі:

```bash
supabase db diff -f create_profiles_table
```

- `-f create_profiles_table` дає назву вашому файлу міграції.

**Що сталося?**
CLI порівняв локальну базу з тим, що було описано у старих міграціях, побачив нову таблицю `profiles` і створив новий файл, наприклад:
`.supabase/migrations/20251114110000_create_profiles_table.sql`

Якщо ви відкриєте цей файл, всередині буде чистий SQL:

```sql
CREATE TABLE public.profiles (
    id uuid NOT NULL,
    username text,
    CONSTRAINT profiles_pkey PRIMARY KEY (id)
);
```

#### Крок 3: Застосування міграцій

Тепер у вас є "рецепт" для створення таблиці `profiles`.

- **Для локальної розробки:** Якщо ви колись повністю "скинете" локальну базу (командою `supabase db reset`), Supabase автоматично прожене всі файли з папки `.supabase/migrations`, і ваша таблиця `profiles` відновиться.
- **Для Production (Vercel):** Ось найважливіше. Щоб застосувати цю зміну до вашої _справжньої_ хмарної бази, ви повинні зв'язати проєкт та "проштовхнути" міграції.

---

## ☁️ Як синхронізувати (push) з хмарною (Production) базою

Це потрібно зробити один раз для зв'язування проєкту, а потім — щоразу, коли ви хочете "задеплоїти" зміни схеми.

1.  **Зв'язування проєкту (Link):**
    Вам потрібно отримати "референс" (ID) вашого хмарного проєкту. Знайдіть його в URL вашої панелі: `https://supabase.com/dashboard/project/[PROJECT_ID]`

    Виконайте команду та вставте ID, коли вас попросять:

    ```bash
    supabase link --project-ref [ВАШ PROJECT_ID]
    ```

    Вона також попросить пароль від вашої хмарної бази даних.

2.  **Деплой міграцій (Push):**
    Після того як ви створили файл міграції (як у Кроці 2) і протестували його локально, виконайте цю команду, щоб застосувати його до хмарної бази:

    ```bash
    supabase db push
    ```

Ця команда завантажить ваші нові файли міграцій у хмарний проєкт і виконає їх. Після цього ваша база на Vercel (Next.js) бачитиме нову таблицю `profiles`.

### Варіанти `db diff`

Для повноти картини:

- `supabase db diff` (без аргументів): Порівнює локальну базу з локальними файлами міграцій.
- `supabase db diff --linked` (або `--remote`): Порівнює вашу локальну базу з вашою **хмарною (linked) базою**. Це корисно, якщо ви хочете побачити, чим ваша локальна розробка відрізняється від production.

Чи хотіли б ви дізнатися, як "витягнути" (pull) схему з уже існуючого хмарного проєкту, якщо ви почали працювати спочатку в хмарі, а потім вирішили налаштувати локальну розробку?

Так, це дуже поширений сценарій. Ви починаєте проєкт, "накидуєте" таблиці та стовпчики прямо у хмарній Supabase Studio (на `supabase.com`), а вже _потім_ вирішуєте налаштувати локальну розробку.

Коли ви запускаєте `supabase init` локально, ваша папка `.supabase/migrations` порожня, але ж вам потрібно якось "клонувати" структуру з хмари.

Для цього існує команда `supabase db pull`.

## 🚀 Як "витягнути" (pull) схему з хмари

Ця команда підключається до вашої хмарної бази даних, аналізує її повну структуру (всі таблиці, функції, політики RLS) і генерує один великий SQL-файл міграції, який описує весь цей стан.

#### Крок 1: Зв'яжіть проєкт (якщо ще не зробили)

Ви не можете "витягнути" щось, не вказавши, звідки.

1.  Знайдіть ваш `[PROJECT_ID]` в URL-адресі панелі Supabase.
2.  Виконайте команду:
    ```bash
    supabase link --project-ref [ВАШ_PROJECT_ID]
    ```
3.  Введіть пароль від вашої хмарної бази даних (той, який ви вказували при створенні проєкту).

#### Крок 2: Виконайте "pull"

Тепер, коли CLI знає, до якого проєкту підключатися, виконайте:

```bash
supabase db pull
```

**Що станеться?**

- CLI підключиться до хмарної бази.
- Він проаналізує всю схему (schema) у `public`.
- Він створить **один новий файл міграції** у вашій папці `.supabase/migrations`. Назва буде містити поточну дату, наприклад, `20251114131500_pull_schema.sql`.

Цей файл буде містити всі `CREATE TABLE`, `CREATE FUNCTION`, `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` і т.д., які необхідні для відтворення вашої хмарної бази з нуля.

#### Крок 3: Запустіть локальне середовище

Тепер, коли у вас є "початкова" міграція, просто запустіть Supabase:

```bash
supabase start
```

Під час запуску `supabase start` автоматично застосує всі міграції зі згаданої папки до вашої (досі порожньої) локальної бази даних у Docker.

**Результат:** Ваша локальна база даних тепер має **точну копію структури** вашої хмари. Ви можете відкрити локальну Studio (`http://127.0.0.1:54323`) і побачити всі свої таблиці.

---

### ⚠️ Важливе зауваження: Перехід на "Local-First"

Після того, як ви зробили `supabase db pull`, ваша "єдина точка правди" (source of truth) щодо структури бази даних **перемістилася з хмарної Studio у ваші локальні файли міграцій**.

З цього моменту дотримуйтесь **нового робочого процесу**:

1.  **НЕ** створюйте таблиці у хмарній Studio (на `supabase.com`).
2.  **Замість цього:**
    - Запустіть `supabase start`.
    - Створіть таблицю в **локальній** Studio (`localhost:54323`).
    - Згенеруйте нову міграцію: `supabase db diff -f "додав_нову_таблицю_posts"`.
    - Застосуйте цю нову міграцію до хмари: `supabase db push`.

Якщо ви порушите цей процес і внесете зміни в хмарі, ваші локальні файли та хмарна база "розійдуться", і вам доведеться вирішувати конфлікти вручну. Команда `db pull` — це, по суті, одноразова операція для старту.
