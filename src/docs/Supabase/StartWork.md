Supabase repo - https://github.com/supabase/supabase

Щоб запустити Supabase локально, вам знадобляться дві основні речі: **Supabase CLI** (інструмент командного рядка) та **Docker**.

Це чудова практика для розробки, оскільки дозволяє вам тестувати зміни, не впливаючи на вашу "живу" (production) базу даних на платформі Supabase. Весь локальний набір Supabase (база даних Postgres, автентифікація, API) буде працювати у Docker-контейнерах на вашій машині.

Ось покроковий процес.

-----

### 1\. 🛠️ Необхідні інструменти

Перш ніж почати, переконайтеся, що у вас встановлено:

1.  **Docker Desktop:** Supabase CLI використовує Docker для запуску всіх своїх сервісів. Docker має бути **встановлений та запущений** у фоновому режимі.
2.  **Supabase CLI:** Це основний інструмент для керування вашим локальним середовищем.

Найпростіший спосіб встановити CLI (оскільки ви вже працюєте з Next.js, у вас має бути Node.js) — це через npm:

```bash
npm install supabase
```

*(Або через Homebrew на macOS: `brew install supabase/tap/supabase`)*

-----

### 2\. 🚀 Покроковий запуск

Ось як ініціалізувати та запустити Supabase у вашому Next.js проєкті.

#### Крок 1: Ініціалізація Supabase

Перейдіть у кореневу директорію вашого Next.js проєкту і виконайте команду:

```bash
supabase init
```

Ця команда створить нову директорію `.supabase` у вашому проєкті. Вона міститиме конфігурацію та (пізніше) ваші міграції бази даних.

#### Крок 2: Запуск локальних сервісів

Тепер найголовніше — запустіть локальне середовище:

```bash
supabase start
```

Ця команда зробить кілька речей:

  * Завантажить необхідні Docker-образи (Postgres, GoTrue, Kong тощо).
  * Запустить їх у контейнерах.
  * Виведе у термінал **важливу інформацію** про ваш локальний екземпляр.

Після успішного запуску ви побачите щось схоже на це:

```
Started Supabase local development setup.

         API URL: http://127.0.0.1:54321
          DB URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres
      Studio URL: http://127.0.0.1:54323
    Inbucket URL: http://127.0.0.1:54324
        anon key: [ВАШ ЛОКАЛЬНИЙ ANON KEY]
service_role key: [ВАШ ЛОКАЛЬНИЙ SERVICE_ROLE KEY]
```

#### Крок 3: Підключення вашого Next.js v15

Тепер вам потрібно "сказати" вашому Next.js додатку, щоб він використовував цей локальний Supabase замість хмарного.

1.  Створіть (або відкрийте) файл `.env.local` у корені вашого Next.js проєкту.

2.  Додайте туди URL та `anon` ключ, які ви отримали з терміналу:

    ```bash
    # .env.local
    NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
    NEXT_PUBLIC_SUPABASE_ANON_KEY=[СКОПІЮЙТЕ ВАШ 'anon key' ЗВІДСИ]
    ```

3.  Перезапустіть ваш Next.js сервер розробки (`npm run dev`).

Тепер ваш додаток на `localhost:3000` буде спілкуватися з Supabase, що працює локально на вашій машині.

-----

### 3\. 💡 Важливі моменти

  * **Локальна студія:** Ви можете відкрити локальну версію Supabase Studio (панель керування), перейшовши за `Studio URL` (зазвичай `http://127.0.0.1:54323`). Там ви можете бачити свої таблиці, так само як у хмарній версії.
  * **Міграції (Migrations):** Це *ключова* відмінність. Коли ви працюєте локально, ви **не повинні** змінювати схему (створювати таблиці, колонки) через локальну Studio. Замість цього ви маєте використовувати міграції.
      * Коли ви готові створити таблицю, ви пишете SQL у файлі в `.supabase/migrations`.
      * Або ви можете використовувати `supabase db diff` для генерації міграцій.
  * **Зупинка:** Щоб зупинити всі локальні сервіси, виконайте:
    ```bash
    supabase stop
    ```
  * **Локальний сервер Postgress:** Будьте уважні щодо стандартного порту підключення Postgress, якщо у вас локально вже
  встановлена субд то можливо вам буде потрібно змінити (Host Port/Out Port)в Docker контейнері якщо ви локально його змінювали.

  **Чому це важливо для Supabase:**
  Конфлікт портів: Коли supabase start запускає Docker-контейнер з PostgreSQL, цей контейнер всередині себе також використовує порт 5432. CLI намагається "прокинути" (map) цей порт назовні.

  **Рішення Supabase CLI**: Команда Supabase CLI вже враховує цю поширену проблему. Саме тому, коли ви запускаєте supabase start, вона за замовчуванням не використовує порт 5432 для зовнішнього доступу.

  Зверніть увагу на вивід, який ми обговорювали:

  DB URL: postgresql://postgres:postgres@127.0.0.1:**54322**/postgres
  Це означає, що Supabase CLI налаштував Docker так:

  Порт на вашій машині (Host Port/Out Port): 54322

  Порт всередині контейнера (Container Port): 5432

  Таким чином, ви можете підключатися до локальної бази Supabase через порт 54322, і це не буде конфліктувати з вашим іншим локальним сервером PostgreSQL, який працює на 5432.

  Висновок: Ваше зауваження — це суть проблеми, яку розробники Supabase CLI вже вирішили за нас, обравши інший порт за замовчуванням (54322). Якби вони цього не зробили, вам довелося б вручну редагувати config.toml або налаштування Docker.
