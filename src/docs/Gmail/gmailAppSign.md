# Gmail App Password

Пароль додатка (App Password) — це 16-значний код, який ви генеруєте у своєму акаунті Google. Він дає "дозвіл" вашому Node.js-серверу лише на одну річ — у вашому випадку, на надсилання пошти.

Перевага: Якщо ваш сервер зламають, зловмисник отримає лише цей пароль, а не ваш основний пароль від Gmail, Google Drive, Google Photos тощо. Ви можете будь-якої миті "скасувати" (видалити) цей пароль додатка, не змінюючи свій основний.

## 🔑 Як його отримати
ВАЖЛИВО: Ця опція доступна тільки якщо у вас увімкнена 2-етапна автентифікація (2-Factor Authentication).

Увімкніть 2-етапну автентифікацію:

Перейдіть до myaccount.google.com/security.

У розділі "Вхід в Google" (Signing in to Google) знайдіть "2-Step Verification" (Двохетапна перевірка) та увімкніть її, якщо вона ще не увімкнена.

Згенеруйте Пароль додатка:

На тій самій сторінці "Security", поверніться до розділу "Вхід в Google".

З'явиться новий пункт: "App Passwords" (Паролі додатків). Натисніть на нього.

Вам може знадобитися знову ввести свій основний пароль.

## Створіть пароль:

У меню "Select app" (Вибрати додаток) -> виберіть "Mail" (Пошта).

У меню "Select device" (Вибрати пристрій) -> виберіть "Other (Custom name)" (Інше (Власна назва)).

Введіть ім'я, яке ви впізнаєте, наприклад, "Nodemailer Server" або "My Node App".

Натисніть "Generate".

Використайте пароль:

Google покаже вам 16-значний пароль у жовтому полі.

Приклад: abcd efgh ijkl mnop

## 👨‍💻 Як використовувати в коді
Скопіюйте цей 16-значний код без пробілів і вставте його у свій код або .env файл як GMAIL_PASS.

```
const nodemailer = require('nodemailer');

// Тепер GMAIL_PASS - це ваш 16-значний Пароль додатка
const GMAIL_USER = 'your.email@gmail.com';
const GMAIL_PASS = 'abcdefghijklmnop'; // 16 символів БЕЗ пробілів

const mailTransport = nodemailer.createTransport({
  service: 'gmail',
  auth: {
    user: GGMAIL_USER,
    pass: GMAIL_PASS, // Сюди йде Пароль додатка
  },
});
```
